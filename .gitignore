# OS / editor junk
.DS_Store
**/.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
backend/.venv/
.venv/
.venv-train/

# Datasets — NOT redistributable (AffectNet research license) + far too large for git.
# Regenerate face crops with backend/crop_faces.py, features with backend/extract_blendshapes.py.
resources/datasets/
data/
*.zip

# Logs + watchdog state
logs/
backend/face_landmarker.task

# NOTE: trained models (app/*.onnx, app/*.pt, app/*_meta.json, app/model.json) ARE committed —
# they're the deliverable. The big raw data above is excluded; models are kept.

# graphify internal cache / interpreter pointers (keep graph.html, graph.json, GRAPH_REPORT.md)
graphify-out/cache/
graphify-out/.graphify_*
graphify-out/cost.json

# CodeGraph index
.codegraph/

# runtime locks
.claude/scheduled_tasks.lock
